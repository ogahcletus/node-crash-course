//The global object in node env is not windows but global
/*
//console.log(global)

//global setTimeout Object

global.setTimeout(() => {
    console.log('Hello Mercy')
}, 2000);


setTimeout(() => {
  console.log('Hello Ene!');
  console.log('Hello Ogeyi')

  clearInterval(int)
    
}, 5000)

// Global setInterval (runs continually at specified interval)

const int = setInterval(() => {
    console.log(`Oh God have Mercy on Me! Arise and command SUDDEN destruction upon all my enemies both spiritual and physical that are determined to ruin and destroy Me!`)
}, 1000);

*/
//Other global objects in nodes:

console.log(__dirname); //output the currebt directory
console.log(__filename); //output the current directory and the filename